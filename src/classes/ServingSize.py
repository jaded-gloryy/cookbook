# multiplier or divisor 
# based on measurement types
#   (cups are bucketed into halves, quarters, eighths (also an integer) maybe a float? bc I want to round)
#   (volume is usually whole integers)
#   (mass can be decimal integer)